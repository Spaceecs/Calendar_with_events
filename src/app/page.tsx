'use client';
import {CalendarWidget} from "@/widgets/calendarWidgets/CalendarWidget";

export default function Home() {
    return (
        <div>
            <CalendarWidget/>
        </div>
    );
}
